ngDescribe({
    name: 'Test charts-inline component',
    modules: 'app',
    element: '<charts-inline></charts-inline>',
    tests: function (deps) {

        it('basic test', () => {
            //
        });
    }
});
